# lotr818/docker-cowsay
Demo docker container

Docker container that runs cowsay with the message "HI!".
Based off of ubuntu

To pull this image: ```docker pull lotr818/docker-cowsay```

Example usage: ```docker run lotr818/docker-cowsay```
